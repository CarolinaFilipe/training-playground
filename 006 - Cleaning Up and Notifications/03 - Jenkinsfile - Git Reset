pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
	    	sh "ls -lta"
		sh "git clean -fd"
                sh "date > date.txt"
		sh "ls -lta"
		
            }
        }
    }
}
