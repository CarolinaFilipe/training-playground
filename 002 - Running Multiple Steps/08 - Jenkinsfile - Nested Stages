pipeline {
    agent none
    stages {
    	stage('build'){
		agent {
			docker{
				image 'maven:3.3.3'
				image 'node:6.3'
			}
		}
		steps {
			sh "mvn --version"
			sh "npm --version"
		}
	}
    }
}
